{
  "name": "@arhor/ember",
  "version": "0.0.0",
  "private": true,
  "engines": { "node": ">=24" },
  "bin": { "ember": "bin/ember.mjs" },
  "scripts": {
    "test": "node --test",
    "test:continuity": "node --test test/*.test.mjs",
    "test:docs": "node --test tests/docs-discovery.test.mjs tests/docs-discovery-repository.test.mjs",
    "check": "node --check bin/ember.mjs && for file in src/ember/*.mjs test/*.mjs test-fixtures/providers/*.mjs; do node --check \"$file\"; done && node scripts/docs-discovery.mjs check"
  }
}
