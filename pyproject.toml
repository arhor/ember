[build-system]
requires = []
build-backend = "ember_build"
backend-path = ["build_support"]

[project]
name = "ember-continuity-slice"
version = "0.1.0"
description = "Ember's experimental minimal continuity vertical slice"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ember = "ember.cli:main"

[tool.ember]
status = "experimental-vertical-slice"
