# References

The `ember-documentation` skill intentionally keeps repository-specific documentation semantics in Ember's canonical source documents rather than duplicating them here.

Read these repository files when the workflow requires their details:

- `docs/documentation-discovery.md` for the governing discovery and metadata contract.
- `docs/documentation-discovery-guide.md` for authoring guidance and command examples.

This directory exists as an extension point for future skill-specific reference material that is not itself Ember documentation governance.
